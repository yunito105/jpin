# CI/CD ガイド — 技術背景から実践まで

> **目的**: CI/CD の「そもそも何なのか」から、本プロジェクトで「何ができるようになるのか」まで、  
> 技術背景を含めて段階的に理解できるようにまとめる。

---

## 目次

1. [CI/CDとは何か](#1-cicdとは何か)
2. [なぜCI/CDが必要なのか — 手動運用の問題点](#2-なぜcicdが必要なのか--手動運用の問題点)
3. [CI/CDの全体像](#3-cicdの全体像)
4. [用語集](#4-用語集)
5. [主要なCI/CDツール比較](#5-主要なcicdツール比較)
6. [GitHub Actions 入門](#6-github-actions-入門)
7. [本プロジェクトで実現できること](#7-本プロジェクトで実現できること)
8. [実践：段階的な導入プラン](#8-実践段階的な導入プラン)
9. [具体的な設定ファイル例](#9-具体的な設定ファイル例)
10. [発展的なトピック](#10-発展的なトピック)
11. [よくある疑問](#11-よくある疑問)

---

## 1. CI/CDとは何か

### 1.1 一言でいうと

> **「コードを書いたら、テスト・ビルド・デプロイを全部自動でやってくれる仕組み」**

手動で毎回やっていた作業を、コードをGitにpushするだけで自動的に実行させる。

### 1.2 CI・CD それぞれの意味

| 略語 | 正式名称 | 意味 | やってくれること |
|------|----------|------|------------------|
| **CI** | Continuous Integration（継続的インテグレーション） | コードを統合するたびに自動検証 | テスト実行、ビルド、コード品質チェック |
| **CD** | Continuous Delivery（継続的デリバリー） | いつでもリリースできる状態を自動で維持 | ステージング環境へのデプロイ、リリース準備 |
| **CD** | Continuous Deployment（継続的デプロイ） | 本番環境まで自動でリリース | 本番サーバーへの自動デプロイ |

```
CI（自動テスト・ビルド）   CD（自動デプロイ）
    ↓                        ↓
┌─────────┐  push   ┌──────────┐  OK   ┌──────────────┐  OK   ┌──────────┐
│ 開発者が │───────→│ 自動テスト │──────→│ 自動ビルド    │──────→│ 自動配置  │
│ コードを │        │ が走る    │       │ が走る       │       │ される   │
│ 書く    │        └──────────┘       └──────────────┘       └──────────┘
└─────────┘              ↓ NG
                    通知が来る
                  「テスト失敗！」
```

### 1.3 身近な例えで理解する

**CI/CDがない場合（手動）:**
```
料理に例えると...

① シェフが料理を作る（= コードを書く）
② 自分で味見する（= ローカルでテスト）
③ 自分でお皿に盛り付ける（= ビルド）
④ 自分でホールに運ぶ（= デプロイ）
⑤ 自分で伝票に記録する（= ドキュメント更新）

→ 全部 手作業。忙しいと味見を忘れたり、伝票を書き忘れたりする
```

**CI/CDがある場合（自動）:**
```
① シェフが料理を作る（= コードを書く）
② → 以降は全部自動でやってくれるロボットがいる
   - 味見ロボ（= テスト自動実行）
   - 盛り付けロボ（= ビルド自動実行）
   - 配膳ロボ（= 自動デプロイ）
   - 記録ロボ（= ドキュメント自動生成）

→ シェフは「料理を作る」ことだけに集中できる
```

---

## 2. なぜCI/CDが必要なのか — 手動運用の問題点

### 2.1 手動でやった場合に起きる問題

| 問題 | 具体例 | 頻度 |
|------|--------|------|
| **テスト忘れ** | 「急いでたからテスト実行せずにpushした」→ バグが混入 | よくある |
| **環境差異** | 「自分のPCでは動くのに...」→ 依存ライブラリのバージョン違い | よくある |
| **ビルド手順の属人化** | 「○○さんしかビルドのやり方を知らない」→ 休むと止まる | ありがち |
| **デプロイミス** | 手動コピーで古いファイルを上書き → 障害発生 | 稀だが致命的 |
| **ドキュメントの陳腐化** | コード変更後にJavadoc再生成を忘れる → 嘘のドキュメント | ほぼ確実 |

### 2.2 CI/CDで解決される世界

```
Before（手動）                          After（CI/CD）
─────────────────                      ──────────────
❌ テスト実行を忘れる                   ✅ 必ず全テストが走る
❌ 人によってビルド手順が違う            ✅ 設定ファイルに統一
❌ デプロイ時にヒヤヒヤする              ✅ テスト通過 → 自動デプロイ
❌ ドキュメントが古いまま                ✅ 毎回自動再生成
❌ 問題発覚が遅い（リリース後）          ✅ pushした直後にわかる
```

---

## 3. CI/CDの全体像

### 3.1 パイプライン（Pipeline）

CI/CDの一連の処理フローを **パイプライン** と呼ぶ。  
水道管（pipe）のように、入力（コード）が流れていくと次々に処理される。

```
┌─────────────────────── CI/CD パイプライン ───────────────────────┐
│                                                                  │
│  ① トリガー     ② ビルド        ③ テスト        ④ デプロイ      │
│  ┌──────────┐  ┌──────────┐   ┌──────────┐   ┌──────────────┐  │
│  │ git push │→│ コンパイル │→ │ 自動テスト │→ │ サーバーに配置 │  │
│  │ PR作成   │  │ npm build │   │ lint/format│   │              │  │
│  │ マージ   │  │ Javadoc   │   │ ArchUnit  │   │ staging      │  │
│  │ 手動実行 │  │ TypeDoc   │   │ JUnit     │   │ → production │  │
│  └──────────┘  └──────────┘   └──────────┘   └──────────────┘  │
│       ↓                            ↓ 失敗                       │
│    「開始」                    「通知（Slack,メール等）」          │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 3.2 本プロジェクトの技術スタックとの対応

```
本プロジェクト構成:
├── demo/           (Spring Boot / Java 17)
│   └── build.gradle
└── frontend/       (Next.js / TypeScript)
    └── package.json

CI/CDで自動化できる作業:
┌───────────────────────────────────────────────────────────────┐
│                                                               │
│  【バックエンド】                 【フロントエンド】            │
│   ./gradlew test                  npm run lint                │
│   ./gradlew build                 npm run build               │
│   ./gradlew javadoc               npm run doc (TypeDoc)       │
│   ./gradlew jigReports                                        │
│                                                               │
│  【統合】                                                     │
│   ./gradlew allDocs   ← 全ドキュメント一括                    │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

---

## 4. 用語集

### 4.1 基本用語

| 用語 | 意味 | 例え |
|------|------|------|
| **パイプライン** | CI/CDの処理フロー全体 | 工場の製造ライン |
| **ワークフロー** | パイプラインの定義ファイル（GitHub Actions用語） | 製造ラインの設計図 |
| **ジョブ (Job)** | パイプライン内の主要な作業単位 | 製造ラインの各工程 |
| **ステップ (Step)** | ジョブ内の個々のコマンド | 工程内の個々の作業 |
| **トリガー** | パイプラインを開始するきっかけ | 製造開始のゴーサイン |
| **ランナー (Runner)** | パイプラインを実際に実行するマシン | 作業を行う工員/ロボット |
| **アーティファクト** | パイプラインで生成される成果物 | 完成品 |

### 4.2 Git関連用語（CI/CDでよく出る）

| 用語 | 意味 |
|------|------|
| **push** | ローカルの変更をリモートリポジトリに送る |
| **Pull Request (PR)** | 変更を本流ブランチに統合するためのレビュー依頼 |
| **マージ (merge)** | PRを承認してブランチを統合する |
| **ブランチ (branch)** | 開発作業を独立して行うための分岐 |
| **main / master** | 本流ブランチ（本番リリース対象） |
| **develop** | 開発用ブランチ |

### 4.3 環境の用語

| 用語 | 意味 | 用途 |
|------|------|------|
| **ローカル環境** | 開発者の手元PC | コーディング・簡易テスト |
| **CI環境** | CI/CDサーバー上の一時的な環境 | 自動テスト・ビルド |
| **ステージング環境** | 本番とほぼ同じ検証環境 | リリース前のテスト |
| **本番環境 (production)** | ユーザーが使う実際の環境 | サービス提供 |

---

## 5. 主要なCI/CDツール比較

### 5.1 ツール一覧

| ツール | 提供元 | 特徴 | 料金(個人/小規模) |
|--------|--------|------|-------------------|
| **GitHub Actions** | GitHub | GitHubと完全統合。最も手軽 | 無料枠あり（2,000分/月） |
| **GitLab CI/CD** | GitLab | GitLabと統合。設定がシンプル | 無料枠あり（400分/月） |
| **Jenkins** | OSS | 自由度最高。自前サーバーが必要 | 無料（サーバー代は別） |
| **CircleCI** | CircleCI | 高速。Docker親和性高い | 無料枠あり |
| **AWS CodePipeline** | AWS | AWS環境と完全統合 | 従量課金 |
| **Azure DevOps** | Microsoft | エンタープライズ向け | 無料枠あり |

### 5.2 どれを選ぶべきか

```
GitHubでコード管理している → GitHub Actions（一択でOK）
GitLabでコード管理している → GitLab CI/CD
社内サーバーで完結させたい → Jenkins
AWSにデプロイしている     → AWS CodePipeline or GitHub Actions
とりあえず試したい        → GitHub Actions（最も情報が多い）
```

**本ドキュメントでは GitHub Actions を前提に解説します。**

---

## 6. GitHub Actions 入門

### 6.1 仕組み

```
┌── GitHub リポジトリ ──────────────────────────────────┐
│                                                       │
│  .github/                                             │
│  └── workflows/                                       │
│      ├── ci.yml         ← テスト・ビルド自動化         │
│      ├── docs.yml       ← ドキュメント自動生成         │
│      └── deploy.yml     ← デプロイ自動化              │
│                                                       │
│  src/  ← 開発者がコードを変更して push                 │
│                                                       │
└──────────────────┬────────────────────────────────────┘
                   │ push / PR
                   ↓
┌── GitHub Actions 実行環境（Runner）─────────────────┐
│                                                     │
│  yml ファイルを読んで順番に実行:                      │
│  ① Java 17 をインストール                           │
│  ② Node.js をインストール                            │
│  ③ ./gradlew test を実行                            │
│  ④ npm run build を実行                              │
│  ⑤ 結果を GitHub に報告                              │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 6.2 ワークフローファイルの基本構造

```yaml
# .github/workflows/ci.yml
name: CI                    # ← ワークフローの名前

on:                         # ← いつ実行するか（トリガー）
  push:                     #    git push したとき
    branches: [main]        #    main ブランチへの push のみ
  pull_request:             #    PR が作られたとき
    branches: [main]

jobs:                       # ← 何を実行するか
  test:                     #    ジョブ名
    runs-on: ubuntu-latest  #    実行環境（Ubuntu Linux）
    steps:                  #    ステップ（個々のコマンド）
      - uses: actions/checkout@v4    # コードを取得
      - name: テスト実行              # ステップ名
        run: ./gradlew test           # 実行するコマンド
```

### 6.3 よく使うトリガー

| トリガー | 説明 | 用途 |
|----------|------|------|
| `push` | コードがpushされたとき | CI（テスト・ビルド） |
| `pull_request` | PRが作成/更新されたとき | コードレビュー前のチェック |
| `schedule` | 定期実行（cron式） | 夜間バッチ、定期テスト |
| `workflow_dispatch` | 手動実行ボタン | 任意のタイミングで実行 |
| `release` | リリースが公開されたとき | 本番デプロイ |

### 6.4 コスト感

| プラン | 月の無料枠 | 超過料金 |
|--------|-----------|----------|
| GitHub Free | 2,000分/月 | $0.008/分 |
| GitHub Pro | 3,000分/月 | $0.008/分 |
| GitHub Team | 3,000分/月 | $0.008/分 |

> 小規模プロジェクト（push 20回/日）で月の利用は概ね 500〜800分程度。**無料枠で十分**。

---

## 7. 本プロジェクトで実現できること

### 7.1 できること一覧

本プロジェクト（Spring Boot + Next.js）で CI/CD に組み込める作業を整理する。

| # | やること | 対象 | コマンド | 所要時間の目安 |
|---|----------|------|----------|---------------|
| 1 | **バックエンドテスト** | Java / JUnit / ArchUnit | `./gradlew test` | 20〜40秒 |
| 2 | **バックエンドビルド** | Spring Boot JAR | `./gradlew build` | 30〜60秒 |
| 3 | **フロントエンドLint** | TypeScript / ESLint | `npm run lint` | 5〜15秒 |
| 4 | **フロントエンドビルド** | Next.js | `npm run build` | 30〜90秒 |
| 5 | **Javadoc生成** | Java → HTML | `./gradlew javadoc` | 5〜10秒 |
| 6 | **JiGレポート生成** | Java → HTML/SVG | `./gradlew jigReports` | 5〜15秒 |
| 7 | **TypeDoc生成** | TypeScript → HTML | `npm run doc` | 3〜5秒 |
| 8 | **全ドキュメント一括** | 上記5+6+7 | `./gradlew allDocs` | 15〜30秒 |

### 7.2 自動化のビフォーアフター

```
Before（今の状態）:
  開発者がコードを書く
  → 自分でテストを実行（忘れるかも）
  → 自分でビルド（手順を覚えてないと困る）
  → 自分で ./gradlew allDocs（忘れるとドキュメントが古いまま）
  → 自分でサーバーにデプロイ（手順が複雑で怖い）

After（CI/CD導入後）:
  開発者がコードを書いて git push する
  → 自動でテスト実行 → 失敗したらSlack通知
  → 自動でビルド
  → 自動でドキュメント生成・公開
  → 自動でステージング環境にデプロイ
  → ✅ 開発者は「コードを書いてpush」だけに集中
```

### 7.3 特に価値が大きいもの

#### ① ArchUnitテストの自動実行（最重要）

本プロジェクトには既に **アーキテクチャルールテスト** が9件ある。  
これがCI/CDで毎push時に自動実行されると：

- レイヤー構造の違反 → **即座に検出**
- 共通ユーティリティの不使用 → **即座に検出**
- PR作成時にテスト結果が表示 → **レビュー前に問題がわかる**

```
開発者がコードを書く
  → push / PR作成
  → CI が自動テスト実行
  → "SLF4Jの直接利用を禁止" のテストが FAIL
  → 「AppLogger を使ってください」とエラーメッセージ表示
  → 開発者が気づいて修正
  → 問題のあるコードが main に混入しない！
```

#### ② ドキュメント自動生成・公開

```
コード変更 → push → CI が ./gradlew allDocs → GitHub Pages に公開
                                                      ↓
                                            チームの誰でも最新の
                                            Javadoc / TypeDoc を
                                            ブラウザで閲覧できる
```

#### ③ フロントエンドのビルドチェック

TypeScriptの型エラーは `npm run build` で検出される。  
CIで毎push時にbuildすることで、**型エラーの混入を防止**できる。

---

## 8. 実践：段階的な導入プラン

### 8.1 推奨する導入順序

一度に全部やろうとせず、段階的に導入する。

```
Phase 1（今すぐ）        超簡単。テストとビルドだけ自動化
    ↓
Phase 2（1〜2週間後）    ドキュメント自動生成を追加
    ↓
Phase 3（慣れたら）      デプロイ自動化、通知連携
```

#### Phase 1: テスト・ビルドの自動化

| 項目 | 内容 |
|------|------|
| 作業時間 | 30分 |
| 必要スキル | YAMLファイルを1個置くだけ |
| 効果 | pushのたびにテストが走り、壊れたらすぐわかる |

#### Phase 2: ドキュメント自動生成

| 項目 | 内容 |
|------|------|
| 作業時間 | 1時間 |
| 必要スキル | Phase 1 + GitHub Pages設定 |
| 効果 | Javadoc/TypeDocが常に最新状態でWeb公開される |

#### Phase 3: デプロイ自動化・通知

| 項目 | 内容 |
|------|------|
| 作業時間 | 2〜4時間 |
| 必要スキル | デプロイ先の環境知識（AWS, Vercel 等） |
| 効果 | マージしたら自動で本番に反映 |

---

## 9. 具体的な設定ファイル例

### 9.1 Phase 1: テスト・ビルド自動化

```yaml
# .github/workflows/ci.yml
# ──────────────────────────────────────────────────
# pushとPR作成時にテスト・ビルドを自動実行する
# ──────────────────────────────────────────────────
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  # ──────────────────────────
  # バックエンド（Java / Spring Boot）
  # ──────────────────────────
  backend:
    name: Backend Test & Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: demo

    steps:
      - name: コードを取得
        uses: actions/checkout@v4

      - name: Java 17 をセットアップ
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Gradle キャッシュ
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('demo/**/*.gradle*', 'demo/gradle-wrapper.properties') }}

      - name: テスト実行（JUnit + ArchUnit）
        run: ./gradlew test

      - name: ビルド
        run: ./gradlew build

      - name: テスト結果をアップロード
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: demo/build/reports/tests/

  # ──────────────────────────
  # フロントエンド（Next.js / TypeScript）
  # ──────────────────────────
  frontend:
    name: Frontend Lint & Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: コードを取得
        uses: actions/checkout@v4

      - name: Node.js をセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 依存パッケージインストール
        run: npm ci

      - name: ESLint 実行
        run: npm run lint

      - name: ビルド（型チェック含む）
        run: npm run build
```

### 9.2 Phase 2: ドキュメント自動生成・公開

```yaml
# .github/workflows/docs.yml
# ──────────────────────────────────────────────────
# mainブランチ更新時にドキュメントを自動生成し
# GitHub Pages に公開する
# ──────────────────────────────────────────────────
name: Generate & Publish Docs

on:
  push:
    branches: [main]
  workflow_dispatch:  # 手動実行も可能

# GitHub Pages へのデプロイ権限
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  docs:
    name: Generate All Docs
    runs-on: ubuntu-latest

    steps:
      - name: コードを取得
        uses: actions/checkout@v4

      - name: Java 17 をセットアップ
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Node.js をセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: フロントエンド依存パッケージインストール
        run: cd frontend && npm ci

      - name: 全ドキュメント一括生成
        run: cd demo && ./gradlew allDocs

      - name: ドキュメントを1つのフォルダに集約
        run: |
          mkdir -p docs-site
          cp -r demo/build/docs/javadoc docs-site/javadoc
          cp -r demo/build/jig docs-site/jig
          cp -r frontend/docs/typedoc docs-site/typedoc

          # トップページ（インデックス）を生成
          cat > docs-site/index.html << 'EOF'
          <!DOCTYPE html>
          <html lang="ja">
          <head><meta charset="UTF-8"><title>店舗ご注文システム - ドキュメント</title>
          <style>
            body { font-family: sans-serif; max-width: 800px; margin: 40px auto; padding: 0 20px; }
            a { display: block; padding: 12px; margin: 8px 0; background: #f5f5f5;
                border-radius: 8px; text-decoration: none; color: #333; }
            a:hover { background: #e0e0e0; }
          </style></head>
          <body>
            <h1>📚 店舗ご注文システム - ドキュメント</h1>
            <a href="javadoc/index.html">📘 Javadoc（バックエンド API仕様）</a>
            <a href="jig/index.html">📊 JiG（ドメインモデル図）</a>
            <a href="typedoc/index.html">📗 TypeDoc（フロントエンド API仕様）</a>
            <p style="color:#888; margin-top:40px;">
              最終更新: 自動生成（GitHub Actions）
            </p>
          </body></html>
          EOF

      - name: GitHub Pages にアップロード
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs-site

  deploy:
    name: Deploy to GitHub Pages
    needs: docs
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: GitHub Pages にデプロイ
        id: deployment
        uses: actions/deploy-pages@v4
```

### 9.3 Phase 3: 通知連携（Slack通知の例）

```yaml
# ci.yml のジョブに追加
  notify:
    name: Slack通知
    needs: [backend, frontend]
    runs-on: ubuntu-latest
    if: failure()  # テスト失敗時のみ通知

    steps:
      - name: Slack にエラー通知
        uses: slackapi/slack-github-action@v2
        with:
          webhook: ${{ secrets.SLACK_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          payload: |
            {
              "text": "❌ CI失敗: ${{ github.repository }} (${{ github.ref_name }})\n変更者: ${{ github.actor }}\n詳細: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            }
```

### 9.4 ファイル配置のまとめ

```
practice/
├── .github/
│   └── workflows/
│       ├── ci.yml          ← Phase 1: テスト・ビルド自動化
│       └── docs.yml        ← Phase 2: ドキュメント自動生成
├── demo/
│   └── build.gradle        ← allDocs タスク（設定済み）
├── frontend/
│   └── package.json        ← doc スクリプト（設定済み）
├── generate-docs.bat       ← ローカル用一括生成（設定済み）
└── generate-docs.sh        ← ローカル用一括生成（設定済み）
```

---

## 10. 発展的なトピック

### 10.1 PRステータスチェック

CIを **必須チェック** に設定すると、テストが通らない限りマージできなくなる。

```
GitHub リポジトリ Settings
  → Branches → Branch protection rules
    → "main" ブランチに追加
      → ☑ Require status checks to pass before merging
        → "Backend Test & Build" を選択
        → "Frontend Lint & Build" を選択
```

**効果**: テストが失敗しているPRは **マージボタンがグレーアウト** される。  
→ 壊れたコードが main に混入することを **仕組みで防止** できる。

### 10.2 キャッシュの活用

CIの実行速度を上げるため、依存ライブラリのキャッシュを設定する。  
（上記のサンプルファイルには設定済み）

| 対象 | キャッシュ | 効果 |
|------|-----------|------|
| Gradle | `~/.gradle/caches` | 2回目以降のビルドが大幅に高速化 |
| npm | `node_modules` | `npm ci` のインストール時間を短縮 |

### 10.3 並列実行

上記サンプルでは `backend` と `frontend` が **並列実行** される。  
互いに依存がないため、同時に処理されてトータル時間が短縮される。

```
                    ┌─ backend: test + build (40秒) ──┐
git push ──→ 開始 ─┤                                  ├→ 完了 (40秒)
                    └─ frontend: lint + build (30秒) ─┘

直列なら 70秒 → 並列で 40秒
```

### 10.4 デプロイ先の選択肢

| デプロイ先 | 対象 | 難易度 | 概要 |
|-----------|------|--------|------|
| **GitHub Pages** | ドキュメント | ★☆☆ | 静的HTML公開。無料 |
| **Vercel** | Next.js | ★☆☆ | GitHub連携でゼロ設定デプロイ |
| **AWS EC2** | Spring Boot | ★★★ | SSH接続して JAR を配置 |
| **AWS ECS** | Docker | ★★★ | コンテナベースのデプロイ |
| **Railway** | Full Stack | ★★☆ | PaaS。設定が少ない |
| **Heroku** | Full Stack | ★★☆ | PaaS。歴史が長い |

### 10.5 セキュリティ：Secrets の管理

APIキーやパスワードは **Secrets** として安全に管理する。

```
GitHub リポジトリ Settings
  → Secrets and variables → Actions
    → "New repository secret"
      → Name: SLACK_WEBHOOK_URL
      → Value: https://hooks.slack.com/services/xxx/yyy/zzz
```

ワークフロー内では `${{ secrets.SLACK_WEBHOOK_URL }}` で参照する。  
**コードに直接書かない**（セキュリティの基本）。

---

## 11. よくある疑問

### Q1. 無料で使えるの？

**GitHub Actions は無料枠で十分使えます。**  
パブリックリポジトリなら無制限。プライベートでも月2,000分の無料枠がある。  
1回のCI実行が約2分なら、**月に1,000回実行可能**。

### Q2. 設定は難しい？

**Phase 1 なら30分でできます。**  
`.github/workflows/ci.yml` ファイルを1つ置くだけ。本ドキュメントのサンプルをコピー＆ペーストすれば動く。

### Q3. ローカルのテスト実行は不要になる？

**ローカルでも引き続きテスト実行することを推奨します。** CIは「最後の砦」。  
ローカルで確認 → push → CI が再確認、の二重チェックが理想。

### Q4. テストがないプロジェクトでも意味ある？

**あります。** テストがなくても以下は自動化できる：
- ビルドが通るかの確認（コンパイルエラー検出）
- ESLint / 型チェック（TypeScript）
- ドキュメント自動生成

### Q5. 本番デプロイまで自動にすると危なくない？

**段階的に導入すれば安全です。**

```
推奨フロー:
  push → CI（自動テスト） 
    → ステージング環境に自動デプロイ（ここまでは自動）
    → 本番デプロイは手動承認（ボタンを押す）
```

GitHub Actions には **「承認を待つ」ステップ** を設定できるため、  
本番デプロイだけは人間の確認を挟むことができる。

### Q6. 1000個のファイルがあると時間がかかる？

**通常は問題ありません。** 大きいOSSプロジェクト（ファイル数千個）でもCIは数分で完了するのが一般的。  
理由：
- Gradle / npm は**差分ビルド**をサポート（変更のないものはスキップ）
- キャッシュ活用で依存ライブラリのダウンロードを省略
- バックエンドとフロントエンドの**並列実行**で時間短縮

---

> **まとめ: CI/CDは「コードを書いてpushするだけで、テスト・ビルド・ドキュメント生成・デプロイが全部自動で行われる」仕組み。  
> 本プロジェクトでは `allDocs` タスクやテスト自動化が既に整っているため、  
> `.github/workflows/ci.yml` を1ファイル追加するだけでCI/CDが始められます。**
